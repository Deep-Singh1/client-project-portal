export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Client Project Portal</h1>
      <p>We will build login, projects, tickets, milestones, and documents.</p>
    </main>
  );
}
